// ApprovalStatus component - deprecated (Web2 migration)
// Token approval is no longer needed in the Web2 architecture

const ApprovalStatus: React.FC = () => {
    // No-op component - token approval removed for Web2 migration
    return null;
};

export default ApprovalStatus;
